<%= render @product %>

<div>
  <%= link_to "Edit this product", edit_product_path(@product) %> |
  <%= link_to "Back to products", products_path %>

  <%= button_to 'Destroy this product', product_path(@product), method: :delete, data: { turbo_confirm: 'Are you sure?' } %>
</div>
